---
description: RKX /loop-plan from findings
---
# /loop-plan

Load `rkx-loop-plan`.

Inspect the relevant target and produce a concise implementation plan. L1 is
read-only unless the USER explicitly authorizes implementation. A run directory
and formal wave are optional unless USER asks. Return the focused plan; do not
emit a five-part wave verdict unless an explicitly requested wave reaches a
terminal decision.
